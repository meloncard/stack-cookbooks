<% (['development', 'production'] + [@environment]).uniq.each do |env| -%>
<%= env %>:
  basic:
    client: <%= @queue[:host] %>
    port: <%= @queue[:port] %>
  work:
    client: <%= @work[:host] %>
    port: <%= @queue[:port] %>
  beanstalk: true

<% end -%>