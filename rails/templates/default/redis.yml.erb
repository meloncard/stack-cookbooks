<% (['development', 'production'] + [@environment]).uniq.each do |env| -%>
<%= env %>:
  session:
    redis: true
    client: <%= @session[:host] %>
    port: <%= @session[:port] %>
  cache:
    redis: false
    client: 
    port: 

<% end -%>
